# Local development: leave this UNSET. API requests stay relative and the Vite
# dev server proxies them to the backend on localhost:4000 automatically.
#
# Production (Vercel): set this to your deployed backend URL, e.g.
# VITE_API_URL="https://your-app.up.railway.app"
